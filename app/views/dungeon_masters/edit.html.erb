<h1>Edit Dungeon Master</h1>

<%= form_with url: "/dungeon_masters/#{@dm_id}", method: :patch, local: true do |form| %>
  <h4><%= form.label :name %></h4>
  <p><%= form.text_field :name %></p>
  <h4><%= form.label 'Number of Players' %></h4>
  <p><%= form.text_field :number_of_players %></p>
  <h4><%= form.label :dm_active %> <%= form.select :dm_active, [true, false] %></h4>
  <h4><%= form.label 'Level Comfortability Range (low, mid, high)' %></h4>
  <p><%= form.text_field :level_range %></p>
  <p><%= form.submit "Update Dungeon Master" %></p>
<% end %>